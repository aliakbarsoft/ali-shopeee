import { SplicNumberCommaDirective } from './splic-number-comma.directive';

describe('SplicNumberCommaDirective', () => {
  it('should create an instance', () => {
    const directive = new SplicNumberCommaDirective();
    expect(directive).toBeTruthy();
  });
});
